// import { Request, Response, Router } from "express";
// import db from "../models/db";

// const endpoints = Router();

// endpoints.get("/users", async (req: Request, resp: Response) => {
//   let r = await db.users.findAll();

//   resp.send(r)
// })

// export default endpoints;